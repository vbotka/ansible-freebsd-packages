---
- name: "Conf: Debug pkg_debug={{ pkg_debug }}"
  when: pkg_debug | bool
  vars:
    msg: |-
      pkg_owner: {{ pkg_owner }}
      pkg_group: {{ pkg_group }}
      pkg_backup_conf: {{ pkg_backup_conf }}
      pkg_default_repo_template: {{ pkg_default_repo_template }}
      pkg_default_repo_conf:
        {{ pkg_default_repo_conf | to_nice_yaml(indent=2) | indent(2) }}
      pkg_repos_template: {{ pkg_repos_template }}
      pkg_repos_conf_clean: {{ pkg_repos_conf_clean }}
      pkg_repos_conf:
        {{ pkg_repos_conf | to_nice_yaml(indent=2) | indent(2) }}
      pkg_repos_conf_ucl:
        {{ pkg_repos_conf_ucl | to_nice_yaml(indent=2) | indent(2) }}
  ansible.builtin.debug:
    msg: "{{ '{}'.format(msg) }}"

- name: "Conf: Configure /etc/pkg/FreeBSD.conf"
  when: pkg_default_repo_conf | length > 0
  ansible.builtin.template:
    src: "{{ pkg_default_repo_template }}"
    dest: /etc/pkg/FreeBSD.conf
    owner: "{{ pkg_owner }}"
    group: "{{ pkg_group }}"
    mode: "0644"
    backup: "{{ pkg_backup_conf }}"

- name: Create repos in /usr/local/etc/pkg/repos/
  when: pkg_repos_conf | length > 0
  block:

    - name: "Conf: Create directories"
      ansible.builtin.file:
        state: directory
        path: "{{ item }}"
        owner: "{{ pkg_owner }}"
        group: "{{ pkg_group }}"
        mode: "0755"
      loop:
        - /usr/local/etc/pkg
        - /usr/local/etc/pkg/repos

    - name: "Conf: Configure /usr/local/etc/pkg/repos"
      register: out
      ansible.builtin.template:
        src: "{{ pkg_repos_template }}"
        dest: /usr/local/etc/pkg/repos/{{ item.name }}.conf
        owner: "{{ pkg_owner }}"
        group: "{{ pkg_group }}"
        mode: "0644"
        backup: "{{ pkg_backup_conf }}"
      loop: "{{ pkg_repos_conf }}"
      loop_control:
        label: "{{ item.name }}"

    - name: "Conf: Debug repos pkg_debug2={{ pkg_debug2 }}"
      when: pkg_debug2 | bool
      ansible.builtin.debug:
        var: out

    - name: "Conf: Debug list repos pkg_debug={{ pkg_debug }}"
      when: pkg_debug | bool
      ansible.builtin.debug:
        msg: |
          {{ out.results | map(attribute='dest') }}

- name: Remove repos in /usr/local/etc/pkg/repos/ not listed in pkg_repos_conf
  when: pkg_repos_conf_clean | bool
  block:

    - name: "Conf: Find repos"
      register: out
      ansible.builtin.find:
        paths: /usr/local/etc/pkg/repos
        patterns: '*.conf'
        file_type: file

    - name: "Conf: Debug repos pkg_debug2={{ pkg_debug2 }}"
      when: pkg_debug2 | bool
      ansible.builtin.debug:
        var: out

    - name: "Conf: Remove repos not listed in pkg_repos_conf"
      vars:
        all_repos: "{{ out.files | map(attribute='path')
                                 | map('basename')
                                 | map('splitext')
                                 | map('first') }}"
        my_repos: "{{ pkg_repos_conf | map(attribute='name') }}"
      ansible.builtin.file:
        state: absent
        path: "/usr/local/etc/pkg/repos/{{ item }}.conf"
      loop: "{{ all_repos | difference(my_repos) }}"

- name: Configure repos
  when: pkg_repos_conf_ucl | length > 0
  block:

    - name: "Conf: Configure repos"
      register: out
      vbotka.freebsd.ucl:
        path: "{{ item.0.name }}"
        create: "{{ item.0.create | d(omit) }}"
        upath: "{{ item.1.key }}"
        state: "{{ item.1.state | d(omit) }}"
        value: "{{ item.1.value | d(omit) }}"
        vtype: "{{ item.1.vtype | d(omit) }}"
        merge: "{{ item.1.merge | d(omit) }}"
        backup: "{{ pkg_backup_conf }}"
      loop: "{{ pkg_repos_conf_ucl | subelements('conf') }}"
      loop_control:
        label: "{{ item.0.name }} {{ item.1.key }}"

    - name: "Conf: Debug repos pkg_debug2={{ pkg_debug2 }}"
      when: pkg_debug2 | bool
      ansible.builtin.debug:
        var: out

    - name: "Conf: Debug list repos pkg_debug={{ pkg_debug }}"
      when: pkg_debug | bool
      ansible.builtin.debug:
        msg: |
          {{ out.results }}

# EOF
